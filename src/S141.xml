<?xml version="1.0" encoding="UTF-8" ?>
<section xml:id="s-Monoids">
<title>Monoids</title>
<idx>Monoids</idx>

<p>Recall that in <xref ref="s-algebraic-systems" text="type-global" /> we introduced systems called monoids. Here is the formal definition.</p>

<definition xml:id="def-monoid"><title>Monoid</title><idx>Monoid</idx>
<statement><p>A monoid is a set <m>M</m> together with a binary operation <m>*</m> with the properties
<ul>
<li><p> <m>*</m> is associative: <m>\forall a,b,c \in M</m>, <m>(a*b)*c=a*(b*c)</m>  and</p></li>
<li><p> <m>*</m> has an identity in <m>M</m>:  <m>\exists e\in M</m> such that <m>\forall a \in M</m>, <m>a*e=e*a=a</m></p></li>
</ul></p></statement></definition> 

<note><p>Since the requirements for a group contain the requirements for a monoid, every group is a monoid.</p></note>

<example xml:id="ex-some-monoids"><title>Some Monoids</title>
<p><ol marker="(a)">
<li><p>  The power set of any set together with any one of the operations intersection, union, or symmetric difference is a monoid.</p></li>
<li><p>  The set of integers, <m>\mathbb{Z}</m>, with multiplication, is a monoid. With addition, <m>\mathbb{Z}</m> is also a monoid.</p></li>
<li><p> The set of <m>n\times n</m> matrices over the integers, <m>M_n(\mathbb{Z})</m>, <m>n\geq 2</m>, with matrix multiplication, is a monoid. This follows from the fact that matrix multiplication is associative and has an identity, <m>I_n</m>. This is an example of a noncommutative monoid since there are
matrices, <m>A</m> and <m>B</m>, for which <m>A B \neq B A</m>.</p></li>
<li><p> <m>\left[\mathbb{Z}_n;\times_n\right],n\geqslant 2</m>, is a monoid with identity 1. </p></li>
<li><p>  Let <m>X</m> be a nonempty set. The set of all functions from <m>X</m> into <m>X</m>, often denoted <m>X^X</m>, is a monoid over function
composition. In Chapter 7, we saw that function composition is associative. The function <m>i:X\to X</m> defined by <m>i(a)=a</m> is the identity element
for this system. If <m>\lvert X\rvert</m> is greater than 1 then it is a noncommutative monoid.
If <m>X</m> is finite, <m>\left\lvert X^X\right\rvert =\lvert X\rvert ^{\lvert X\rvert }</m> . For example, if <m>B=\{0,1\}, \left\lvert B^B\right\rvert=4</m>. The functions <m>z,u,i,\textrm{ and } t,</m> defined by the graphs in <xref ref="fig-functions-on-B2" text="type-global" />, are the elements of <m>B^B</m> . This monoid is not a group. Do you know why?</p>
<p>One reason why <m>B^B</m> is noncommutative is that <m>t \circ z \neq z \circ t</m> because <m>(t\circ z)(0)=t(z(0))=t(0)=1</m> while <m>(z\circ t)(0)=z(t(0))=z(1)=0</m>.</p></li>
</ol></p>
</example>
            <figure xml:id="fig-functions-on-B2">
                <caption>The functions on <m>B_2</m>
                </caption>
                <image width="70%" source="images/fig-functions-on-B2.png">
                    <description><p>The functions on <m>B_2</m></p></description>
                </image>
            </figure>
<p>Virtually all of the group concepts that were discussed in Chapter 11 are applicable to monoids. When we introduced subsystems, we saw that a submonoid of monoid <m>M</m> is a subset of <m>M</m>; that is, it is a monoid with the operation of <m>M</m>. To prove that a subset is a submonoid, you can apply the following theorem.</p>

<theorem xml:id="th-submonoid"><title>Submonoid Test</title>
<statement><p>Assume <m>[M; *]</m>  is a monoid and <m>K</m> is a nonempty subset of <m>M</m>. Then <m>K</m> is a submonoid of <m>M</m> if and only if the following two conditions are met.
<ul>
<li><p>If <m>a,b\in K</m>, then.  <m>a*b\in K</m>; i. e., <m>K</m>  is closed with under <m>*</m>.</p></li>
<li><p>The identity of <m>M</m> belongs to <m>K</m>.</p></li>
</ul></p></statement>
</theorem>

<p>Often we will want to discuss the smallest submonoid that includes a certain subset <m>S</m> of a monoid <m>M</m>. This submonoid can be defined recursively by the following definition.</p>

<definition xml:id="def-submonoid-generated-by-a-set"><title>Submonoid Generated by a Set</title><idx><h>Submonoid</h><h>Generated by a Set</h></idx>
<statement><p>If <m>S</m> is a subset of monoid <m>[M;*]</m>, the submonoid generated by <m>S</m>, <m>\langle S\rangle</m>, is defined by:. <ol marker="(a)">
<li><p>  (Basis) The identity of <m>M</m> belongs to <m>\langle
S\rangle</m>; and
 <m>a\in S \Rightarrow a\in \langle S \rangle</m>. </p></li>
<li><p> (Recursion) <m>a,b\in \langle S\rangle \Rightarrow a*b\in \langle S\rangle</m>.</p></li></ol></p></statement></definition>

 <note><p> If <m>S=\left\{a_1,a_2,\ldots ,a_n\right\}</m>, we write <m>\left\langle a_1,a_2,\ldots ,a_n\right\rangle</m> in place of <m>\left\langle \left\{a_1,a_2,\ldots
,a_n\right\}\right\rangle</m>.</p></note>

<example xml:id="ex-some-submonoids"><title>Some Submonoids</title><p><ol marker="(a)">
<li><p>One example of a submonoid of <m>[\mathbb{Z};+]</m> is <m>\langle 2\rangle =\{0,2,4,6,8,\ldots \}</m>.</p></li>
<li><p> The power set of <m>\mathbb{Z}</m>, <m>\mathcal{P}(\mathbb{Z})</m>, over union is a monoid with identity <m>\emptyset</m>. If <m>S=\{\{1\},\{2\},\{3\}\}</m>, then <m>\langle S \rangle</m> is the power set of <m>\{1,2,3\}</m>. If <m>S=\{\{n\}:n\in \mathbb{Z}\},</m> then <m>\langle S\rangle</m> is the set of finite subsets of the integers.</p></li>
</ol>
</p></example>

<p>As you might expect, two monoids are isomorphic if and only if there exists a translation rule between them so that any true proposition in one monoid is translated to a true proposition in the other.</p>

<example xml:id="ex-14-1-iso">
<p><m>M=[\mathcal{P}\{1,2,3\};\cap  ]</m> is isomorphic to <m>M_2=\left[\mathbb{Z}_2^3;\cdot \right]</m>, where the operation in <m>M_2</m> is componentwise mod 2 multiplication. A translation rule is that if <m>A\subseteq \{1,2,3\}</m>, then it is translated to <m>\left(d_1,d_2,d_3\right)</m> where 
<me>d_i=\left\{
\begin{array}{cc}
 1 &amp; \textrm{ if } i\in A \\
 0 &amp; \textrm{ if } i\notin A \\
\end{array}
\right.</me> 

 Two cases of how this translation rule works are:

<me>
\begin{array}{lr}
			\begin{array}{c}
			\{1, 2, 3\}\quad\textrm{ is the identity for } M_1\\
			 \updownarrow   \\
			(1,1,1) \quad\textrm{ is the identity for }M_2\\
			\end{array} 
	&amp;     
		\begin{array}{c}
		 \{1, 2\}\cap \{ 2, 3\}=\{2\}\\
			\updownarrow \\       
		(1, 1, 0) \cdot  (0, 1, 1) = (0, 1, 0)\\
		\end{array}\\
               \quad
\end{array}
</me>.</p></example>  

<p>A more precise definition of a monoid isomorphism is identical to the definition of a group isomorphism, <xref ref="def-group-isomorphism" />.</p>

<exercises xml:id="exercises-14-1">
<title>Exercises</title>
<exercise number="1"><statement><p>  For each of the subsets of the indicated monoid, determine whether the subset is a submonoid.
<ol marker="(a)">
<li><p> <m>S_1=\{0,2,4,6\}</m> and  <m>S_2=\{1,3,5,7\}</m> in <m>[\mathbb{Z}_8;\times_8].</m></p></li>
<li><p> <m>\{f\in \mathbb{N}^{\mathbb{N}}:f(n) \leqslant n, \forall n \in \mathbb{N}\}</m> and  <m>\left\{f\in \mathbb{N}^{\mathbb{N}}:f(1)=2\right\}</m> in the monoid <m>[\mathbb{N}^{\mathbb{N}};\circ]</m>.</p></li>
<li><p> <m>\{A\subseteq \mathbb{Z} \mid A \textrm{ is finite}\} \textrm{ and} \left\{A\subseteq \mathbb{Z} \mid A^c\textrm{ is} \textrm{ finite}\right\}</m> in <m>[\mathcal{P}(\mathbb{Z});\cup].</m></p></li>
</ol>
</p></statement>
<answer><p>
<ol marker="(a)">
<li><p> <m>S_1</m> is not a submonoid since the identity of <m>\left[\mathbb{Z}_8; \times_8\right]</m>, which is 1, is not in <m>S_1</m>.   <m>S_2</m> is a submonoid since <m>1 \in  S_2</m> and <m>S_2</m> is closed under multiplication; that is, for all <m>a, b \in  S_2</m>, <m>a \times_8 b</m> is in <m>S_2</m>.</p></li>
<li><p>The identity of <m>\mathbb{N}^{\mathbb{N}}</m> is the identity function <m>i:\mathbb{N}\to \mathbb{N}</m> defined by <m>i(a) = a</m>, <m>\forall a\in \mathbb{N}</m>.  If <m>a \in \mathbb{N}</m>, <m>i(a) = a \leq  a</m>, thus the identity of <m>\mathbb{N}^{\mathbb{N}}</m> is in <m>S_1</m>. However, the image of 1 under any
function in <m>S_2</m> is 2, and thus the identity of <m>\mathbb{N}^{\mathbb{N}}</m> is not in <m>S_2</m>, so <m>S_2</m> is not a submonoid. The composition of any two functions in <m>S_1</m>,  <m>f</m> and <m>g</m>, will be a function in <m>S_1</m>:
<me>
\begin{split}
(f\circ g)(n) &amp; = f(g(n)) \leq g(n)\textrm{      since } f \textrm{ is in } S_1\\
		&amp; \leq n\textrm{       since } g \textrm{ is in } S_1 \Rightarrow f \circ g \in S_1
\end{split}
</me>
and the two conditions of a submonoid are satisfied and <m>S_1</m> is a submonoid of  <m>\mathbb{N}^{\mathbb{N}}</m>.</p></li>
<li><p>The first set is a submonoid, but the second is not since the null set has a non-finite complement.</p></li>
</ol>
</p></answer>
</exercise>
<exercise number="2"><statement><p>  For each subset, describe the submonoid that it generates.
<ol marker="(a)">
<li><p>  <m>\{3\}</m> in <m>[\mathbb{Z}_{12};\times_{12}]</m></p></li>
<li><p>  <m>\{5\} \textrm{ in } [\mathbb{Z}_{25};\times_{25}]</m></p></li>
<li><p>  the set of prime numbers in <m>[\mathbb{P}; \cdot ]</m></p></li>
<li><p> <m>\{3,5\} \textrm{ in } [\mathbb{N}; +]</m></p></li>
</ol>
</p></statement></exercise>
<exercise number="3"><statement><p>An <m>n \times n</m>  matrix of real numbers is called <term>stochastic</term> if and only if each entry is nonnegative and the sum of entries in each column is 1. Prove that the set of stochastic matrices is a monoid over matrix multiplication.</p></statement>
<answer><p>The set of <m>n \times  n</m> real matrices is a monoid under matrix multiplication. This follows from the laws of matrix algebra in Chapter 5. To prove that the set of stochastic matrices is a monoid over matrix multiplication, we need only show that the identity matrix is stochastic (this is obvious) and that the set of stochastic matrices is closed under matrix multiplication. Let <m>A</m> and <m>B</m> be <m>n \times  n</m> stochastic matrices.
 <me>(AB)_{i j}= \sum _{k=1}^n a_{i k} b_{k j}</me>
</p>
<p>The sum of the <m>j^{\textrm{th}}</m> column is
<me>
\begin{split}
\sum_{j=1}^n (AB)_{i j} &amp; =\sum_{k=1}^n a_{1 k} b_{k j}+\sum_{k=1}^n a_{1k} b_{k j}+\cdots +\sum_{k=1}^n a_{n k} b_{k j}\\
	&amp;=\sum_{k=1}^n \left(a_{1 k} b_{k j}+a_{1k} b_{k j}+\cdots +a_{n k} b_{k j}\right)\\
	&amp;=\sum _{k=1}^n b_{k j}\left(a_{1 k} +a_{1k}+\cdots +a_{n k} \right)\\
	&amp;= \sum _{k=1}^n  b_{k j} \quad\textrm{         since } A \textrm{ is stochastic}\\
	&amp; = 1 \quad\textrm{         since } B \textrm{ is stochastic}\\
\end{split}
</me>
</p></answer>
</exercise>
<exercise number="4"><statement><p> A <term>semigroup</term> is an algebraic system <m>[S; *]</m> with the only axiom that <m>*</m> be associative on <m>S</m>. Prove that if <m>S</m> is a finite set, then there must exist an idempotent element, that is, an <m>a \in  S</m> such that <m>a * a = a</m>.</p></statement>
</exercise>
<exercise number="5"><statement><p>  Let <m>B</m> be a Boolean algebra and <m>M</m> the set of all Boolean functions on <m>B</m>. Let <m>*</m> be defined on <m>M</m> by <m>(f * g)(a) = f(a) \land  g(a)</m>. Prove that <m>[M;*]</m> is a monoid. Construct the operation table of <m>[M;*]</m> for the case of <m>B = B_2</m>.</p>
</statement>
<answer><p> Let <m>f, g, h \in  M</m>, and <m>a \in  B</m>.
<me>
\begin{split}
((f*g)*h)(a) &amp; = (f*g)(a) \land  h(a)\\
		&amp; =  (f(a)\land g(a))\land h(a)\\
		&amp; =  f(a) \land ( g(a) \land  h(a))\\
		&amp; =  f(a) \land  (g * h)(a)\\
		&amp; =  (f * (g * h))(a)\\
\end{split}
</me>

Therefore <m>(f * g) * h =f * (g * h)</m> and  <m>*</m> is  associative.</p>
<p>The identity for <m>*</m> is the function <m>u \in  M</m> where <m>u(a) = 1</m> = the <q>one</q> of <m>B</m>. If <m>a \in  B</m>,
<m>(f*u)(a) =f(a)\land u(a) = f(a)\land 1 = f(a)</m>. Therefore <m>f * u = f</m>. Similarly, <m>u * f =f</m>.</p>
<p>There are <m>2^2= 4</m> functions in <m>M</m> for <m>B = B _2</m>. These four functions are named in the text. See <xref ref="fig-functions-on-B2" text="type-global" />. The table for <m>*</m> is

<me>\begin{array}{c|cccc}
  * &amp;  z &amp;  i &amp; t &amp;  u\\
\hline
 z &amp;z &amp; z &amp; z &amp; z \\
 i &amp;z &amp; i &amp; z &amp; i \\
 t &amp;z &amp; z &amp; t &amp; t \\
 u &amp;z &amp; i &amp; t &amp; u \\
\end{array}</me>
</p></answer>

</exercise>
</exercises>
</section>

